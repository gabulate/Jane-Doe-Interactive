@model IEnumerable<Infrastructure.Models.Reservacion>

@{
    ViewBag.Title = "Index";
}

<h2>Mis reservaciones</h2>

<p>
    <a href="@Url.Action("Create", "Reservacion")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Agregar </a>
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AreaComun.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoraInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoraFinal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AreaComun.Descripcion)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.HoraInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoraFinal)
        </td>
    </tr>
}

</table>
